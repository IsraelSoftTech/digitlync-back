const express = require('express');
const router = express.Router();

// Placeholder - to be implemented per SRS Booking workflow
router.get('/', (req, res) => {
  res.json({ message: 'Bookings endpoint - Phase 1' });
});

module.exports = router;
