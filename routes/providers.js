const express = require('express');
const router = express.Router();

// Placeholder - to be implemented per SRS Provider Data Model
router.get('/', (req, res) => {
  res.json({ message: 'Providers endpoint - Phase 1' });
});

module.exports = router;
